もちろんです！以下は、日本語版の内容を正確かつ自然な英語に翻訳した **English version of README.md** です。ヘッダーの言語リンクも英語版用に適切に調整しています。

---

# rain-focus

>[!WARNING]  
> This README.md was generated by generative AI.

A simple website that plays **ambient rain sounds** using the **Web Audio API**.  
It preloads the audio and supports fade-in/fade-out, volume persistence, and seamless looping.

---

## [日本語](../../README.md) | [한국어](../korean.md) | [Français](../french.md) | **English**

## ✨ Features

- Loop playback of rain sound (`audio.mp3`)  
- **Fade-in** on start (2 seconds)  
- **Fade-out** on stop (2 seconds)  
- Real-time volume control via slider  
- Previous volume level automatically saved to `localStorage`  
- Built with **Web Audio API** (`AudioBufferSourceNode`, `GainNode`)

---

## 📁 Project Structure (example)

```
project/
├── index.html
├── script.ts
├── audio.mp3
└── README.md
```

---

## 🚀 How to Use

1. Replace `audio.mp3` with your preferred rain sound file.  
2. Open `index.html` in your browser.  
3. Click **Start** to play, **Stop** to pause.  
4. Adjust volume with the slider — settings are saved automatically.

---

## 🛠 Technologies Used

- TypeScript  
- Web Audio API  
  - `AudioContext`  
  - `AudioBufferSourceNode`  
  - `GainNode`  
- Fetch API / `decodeAudioData`  
- `localStorage`

---

## 🔧 Key Implementation Details

- A new `AudioBufferSourceNode` is created on every playback using `createBufferSource()`  
- Fade effects are handled through a `GainNode`  
- Volume changes are applied instantly with `gain.setValueAtTime()`  
- Fades use `linearRampToValueAtTime()` for smooth transitions  

---
